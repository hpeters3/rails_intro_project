<%= render partial: "games/menu" %>
<h1><%= @show.name%></h1>
<table>
    <tr>
        <td>Description: <%= @show.description%></td>
        <% if @show.game_uuid.present? %>
            <td>First apperance: <%= link_to @show.game_uuid, game_path(@show.game_uuid) %></td>
        <% end %>
    </tr>
<table>